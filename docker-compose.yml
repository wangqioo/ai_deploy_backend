version: '3'
services:
  xiaozhi-backend:
    build: .
    container_name: xiaozhi-backend
    restart: unless-stopped
    ports:
      - "8090:8088"
    env_file: .env
    environment:
      - TZ=Asia/Shanghai
